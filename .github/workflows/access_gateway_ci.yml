name: 🚪 Access Gateway CI

on:
  pull_request:
    branches: [ "dev" ]
    paths:
      - 'access_gateway/**'
      - 'common/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    env:
      LISTING_SERVICE_URL: ${{ vars.LISTING_SERVICE_URL }}
      BOOKING_SERVICE_URL: ${{ vars.BOOKING_SERVICE_URL }}
      PAYMENT_SERVICE_URL: ${{ vars.PAYMENT_SERVICE_URL }}
      MEMBER_SERVICE_URL: ${{ vars.MEMBER_SERVICE_URL }}
      IMAGE_SERVICE_URL: ${{ vars.IMAGE_SERVICE_URL }}
      MESSAGING_SERVICE_URL: ${{ vars.MESSAGING_SERVICE_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Run access_gateway tests and generate JaCoCo report
        run: ./gradlew clean :access_gateway:jacocoTestReport --no-daemon

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: ${{ github.repository }}
          files: ./access_gateway/build/reports/jacoco/test/jacocoTestReport.xml
          flags: access_gateway
