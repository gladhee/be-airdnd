name: 🖼️ Image Service CI

on:
  pull_request:
    branches: [ "dev" ]
    paths:
      - 'image/**'
    paths-ignore:
      - 'common/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Run image tests
        run: ./gradlew :image:test

      - name: Run image tests and generate JaCoCo report
        run: ./gradlew clean :image:test:jacocoTestReport --no-daemon

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: ${{ github.repository }}
          files: ./image/build/reports/jacoco/test/jacocoTestReport.xml
          flags: image
